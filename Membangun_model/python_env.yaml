name: training_env
channels:
  - defaults

dependencies:
  - python=3.10
  - pip:
      - pandas
      - scikit-learn
      - mlflow
      - prometheus-client
      - fastapi
      - joblib
      - uvicorn
      - tqdm
